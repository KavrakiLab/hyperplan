planner_configs:
  planner:
    type: geometric::{planner}
{params}
arm_with_torso:
  planner_configs:
    - planner
  projection_evaluator: joints(torso_lift_joint,shoulder_pan_joint)
  longest_valid_segment_fraction: 0.005
